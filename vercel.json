{
    "version": 2,
    "name": "top-auto-brand",
    "builds": [
       { 
        
       "src": "index.js",
        "use": "@vercel/node"
     }
    ],
    "routes": [
       { 
         "src": "/(.*)",
         "dest": "/",
         "methods": ["GET", "OPTIONS" , "POST", "PATCH", "PUT", "DELETE"]
        }
    ]
 }